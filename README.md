# SkiFree Three.js remake

Run it with:

```powershell
cd skifree-three
npm.cmd install
npm.cmd run dev -- --port 5173
```

Build it with:

```powershell
cd skifree-three
npm.cmd run build
```

Verify rendering and gameplay behavior with:

```powershell
cd skifree-three
npm.cmd run verify
```

